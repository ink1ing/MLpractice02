# 玉米南方锈病遥感识别系统

本项目基于深度学习技术，构建玉米南方锈病的遥感识别模型，实现对感染部位和感染等级的自动识别。通过多任务学习方法，同时实现两个目标：

1. **感染部位分类**：下部/中部/上部 (3分类)
2. **感染等级判断**：无/轻度/中度/重度/极重度 (对应病害等级：0/3/5/7/9)

## 项目概述

项目使用无人机获取的多光谱图像(.tif)和人工标注文件(.json)，通过深度学习模型实现玉米南方锈病的智能识别。项目分为三个阶段：

1. **阶段一（已完成）**：使用14叶片数量的小样本验证模型框架和训练流程
2. **阶段二（已完成）**：扩展到9个文件夹数据集（9l/m/t、14l/m/t、19l/m/t），每类101张，共909张样本对，并实现ResNet模型
3. **阶段三（当前）**：实现FCN全卷积网络模型，进行像素级分类，提高识别精度和可解释性

## 项目更新说明

### 最新更新
我们已完成了从阶段二（基于ResNet的分类网络）到阶段三（基于FCN的分割网络）的升级，新增了以下关键功能：

1. **FCN全卷积网络实现**：
   - 实现编码器-解码器结构，保留空间信息
   - 通过跳跃连接提升特征保留能力
   - 引入注意力门控机制，突出关键区域

2. **训练框架优化**：
   - 实现混合精度训练，加速模型训练
   - 添加早停策略，避免过拟合
   - 实现训练过程性能监控和可视化

3. **多系统支持**：
   - 提供Windows（.bat）和Unix（.sh）系统启动脚本
   - 自动优化GPU内存使用，提高训练稳定性

## 数据结构

### 图像数据
- 多光谱遥感图像（.tif格式）
- 图像维度：[500, H, W]，其中500是光谱通道数
- 存储路径：`./guanceng-bit/`目录下，按叶片和位置分为9个子目录：
  - 9l, 9m, 9t（9叶期下/中/上部）
  - 14l, 14m, 14t（14叶期下/中/上部）
  - 19l, 19m, 19t（19叶期下/中/上部）

### 标注数据
- JSON格式标注文件，与图像文件一一对应
- 存储路径：`./biaozhu_json/`目录下，按叶片和位置分为9个子目录（与图像目录对应）
- 标注内容：
  - 感染部位：通过文件路径中的l/m/t标识（下/中/上部）
  - 感染等级：通过标签名称中的数字标识（0/3/5/7/9）

## 模型架构

项目实现了两种主要的模型架构：

1. **FCN（全卷积网络）**（`DiseaseFCN`）：**当前主要使用的模型**
   - 编码器-解码器结构，用于像素级分类
   - 注意力门控机制，突出感染区域
   - 同时输出位置分类和等级回归结果

2. **ResNet相关模型**（仅作为参考）：
   - **SimpleNN**：基础卷积神经网络，双头输出
   - **ResNet**：基于残差连接的深度网络
   - **ResNet+**：增强版ResNet，加入通道和空间注意力机制

所有模型都采用多任务学习方法，同时预测：
- 感染部位（3分类问题）：下部/中部/上部
- 感染等级（回归问题）：0-9范围内的连续值

## 项目结构

- **model.py**: 模型定义文件，包含FCN和ResNet系列网络架构
- **dataset.py**: 数据集加载和预处理，处理TIF图像和JSON标注
- **utils.py**: 工具函数，包含损失函数、指标计算等
- **run_fcn.py**: FCN模型训练脚本
- **visualize_fcn.py**: FCN模型结果可视化脚本
- **check_fcn_startup.py**: FCN模型训练前环境检查工具
- **train_fcn.bat**: Windows系统FCN训练启动脚本
- **train_fcn.sh**: Unix系统FCN训练启动脚本
- **check_tif_images.py**: TIF图像检查工具，用于分析图像结构

## 快速开始

### 环境准备
```bash
pip install torch torchvision rasterio scikit-image matplotlib tqdm numpy pillow
```

### 检查图像数据
```bash
python check_tif_images.py --data_root ./guanceng-bit --sample_count 5 --visualize
```

### 环境和数据检查
```bash
python check_fcn_startup.py --data_root ./guanceng-bit --json_root ./biaozhu_json
```

### 启动FCN模型训练
```bash
# Windows系统
train_fcn.bat

# Linux/Unix系统
./train_fcn.sh

# 或直接使用Python命令
python run_fcn.py --data_root ./guanceng-bit --json_root ./biaozhu_json --batch_size 8 --output_dir ./output_fcn --img_size 128
```

### 可视化分割结果
```bash
python visualize_fcn.py --data_root ./guanceng-bit --json_root ./biaozhu_json --model_path ./output_fcn/best_model.pth --output_dir ./fcn_visualization
```

## 训练参数配置

FCN模型的最佳参数配置：
- **批次大小**：8-32（根据GPU内存调整）
- **图像大小**：128x256（可根据需要调整）
- **学习率**：0.0001
- **权重衰减**：1e-6
- **训练轮数**：50（使用早停策略）
- **任务权重**：位置分类0.7，等级回归0.3
- **优化器**：Adam
- **学习率调度**：余弦退火或ReduceLROnPlateau
- **混合精度训练**：启用
- **监控指标**：F1分数
- **早停耐心轮数**：5轮

## 性能指标

模型性能通过以下指标评估：
- **位置分类**：准确率、F1分数、精确率、召回率、混淆矩阵
- **等级预测**：平均绝对误差(MAE)、±2误差容忍率
- **分割质量**：IoU（交并比）、Dice系数

## 常见问题

**Q: 如何处理不同波段数的.tif文件?**  
A: 数据集类会自动处理不同波段数的.tif文件，如果通道数小于3，会复制现有通道；如果大于3，会选择代表性通道。

**Q: 无法读取.tif文件怎么办?**  
A: 确保安装了rasterio库并有适当的GDAL支持。如果遇到读取问题，检查.tif文件的格式和完整性。

**Q: 如何调整两个任务的损失权重?**  
A: 在训练脚本中可以通过pos_weight参数调整，默认为0.7，表示位置分类任务占70%，等级回归任务占30%。

**Q: FCN模型与ResNet模型的主要区别是什么?**  
A: FCN保留空间信息，能够生成分割图并定位病害区域，而ResNet直接对整图分类，空间定位能力较弱。

## FCN模型优势

FCN模型相比传统分类网络具有以下优势：

1. **像素级分类**：可生成精确的分割图，识别病害的精确位置和范围
2. **空间信息保留**：通过跳跃连接保留多尺度特征，增强感染区域识别能力
3. **注意力引导**：通过注意力机制自动关注重要区域，减少背景干扰
4. **更好的可解释性**：生成的分割图和注意力图提供了可视化解释
5. **性能提升**：在维持计算效率的同时提高分类准确率和等级预测精度

## 后续优化方向

1. **多光谱信息利用**：更有效地利用多光谱数据的丰富信息
2. **模型轻量化**：压缩模型大小，提高推理速度，适应边缘设备
3. **预训练策略**：探索在相似任务上的预训练，提高迁移学习效果
4. **自监督学习**：利用未标注数据提高模型泛化能力
5. **模型集成**：尝试多模型集成方法，提高预测稳定性